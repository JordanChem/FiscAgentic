# ─── Développement : UI de debug Streamlit, évaluation, tests ─────────────────
# Ne pas installer sur le serveur de production.

-r requirements.txt

# UI de debug interne (client du même pipeline que l'API)
streamlit>=1.28.0
streamlit-cookies-controller==0.0.4

# Évaluation qualité du pipeline (golden dataset + comparaison de modèles)
deepeval>=2.0.0
openpyxl>=3.1.0          # eval/dataset.py : lecture des golden sets .xlsx

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # client de test ASGI pour l'API
